/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "www.pngplay.com",
            "o.remove.bg",
            "www.pngarts.com",
        ],
    }
}

module.exports = nextConfig
